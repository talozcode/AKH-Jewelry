version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
    groups:
      # Next.js and its own ESLint config must move together - a mismatched
      # pair is a common source of confusing lint errors.
      nextjs:
        patterns:
          - "next"
          - "eslint-config-next"
